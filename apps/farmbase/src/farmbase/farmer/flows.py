# @background_task
async def farmer_init_flow(*, farmer_id: int, organization_slug: str, db_session=None):
    """Initializes a new farmer with default settings."""
    # farmer = await get(db_session=db_session, farmer_id=farmer_id)
    ...
